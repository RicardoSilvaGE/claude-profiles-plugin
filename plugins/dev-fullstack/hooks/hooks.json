{
  "_comment": "Source canonique des QUATRE hooks des profils empaquetes en plugin. Copies dans plugins/<profil>/hooks/ par scripts/build-plugin.sh — NE JAMAIS EDITER LA COPIE, elle est ecrasee a chaque generation et comparee par --check.",

  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "_comment": "Rend a la doctrine du profil la RESIDENCE qu'un skill n'a pas. Timeout 10 s : le script ne fait que lire un fichier local et le passer a jq.",
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/injecter-doctrine.sh",
            "timeout": 10
          }
        ]
      }
    ],

    "PreToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "_comment": "Regle absolue spec-builder (19.05.2026) rendue opposable en session cloud, ou le hook PowerShell du poste ne peut pas tourner. Decision 'ask' UNIQUEMENT, jamais 'deny', ET additionalContext : hors du mode 'default', 'ask' n'interrompt pas, seul le contexte injecte subsiste. Un seul rappel par session et par depot. Ne voit pas les ecritures faites via Bash.",
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/guard-spec-code.sh",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "_comment": "Regle 0 de guard-poste.ps1 : deny sur tout git push direct vers main/master (reference explicite, refspec, --all/--mirror, push nu depuis main). Meme decision qu'au poste, meme effet dans tous les modes. Exemptions par ~/.claude/.push-main-allowed. Ne voit que l'outil Bash.",
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/guard-push-main.sh",
            "timeout": 10
          }
        ]
      }
    ],

    "Stop": [
      {
        "hooks": [
          {
            "_comment": "Filet du PREMIER etage de la regle spec-builder (< 10 lignes : pas de spec, le code doit compiler). Lance tsc --noEmit quand la session a laisse des .ts/.tsx modifies, et bloque la fin de tour avec les erreurs. Un block sur Stop n'est PAS une decision de permission : meme effet en cloud qu'au poste. Timeout 100 s, SUPERIEUR au timeout interne de 90 s du script — l'inverse tuerait le hook avant son verdict. Une seule fois par session et par depot.",
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-build-ts.sh",
            "timeout": 100
          }
        ]
      }
    ]
  }
}
